export class ApiRateLimiter{private readonly buckets=new Map<string,{tokens:number,at:number}>();private readonly capacity:number;private readonly refillPerSecond:number;constructor(capacity=120,refillPerSecond=2){this.capacity=capacity;this.refillPerSecond=refillPerSecond;}consume(key:string,cost=1,now=Date.now()):boolean{const old=this.buckets.get(key)??{tokens:this.capacity,at:now};const tokens=Math.min(this.capacity,old.tokens+Math.max(0,now-old.at)/1000*this.refillPerSecond);if(tokens<cost){this.buckets.set(key,{tokens,at:now});return false;}this.buckets.set(key,{tokens:tokens-cost,at:now});return true;}}
