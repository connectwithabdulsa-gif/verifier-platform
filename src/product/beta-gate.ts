export interface ProviderGate{provider:string;samples:number;falsePositiveRate:number;falseNegativeRate:number;bounceRate:number;}
export interface GateThresholds{minimumSamples:number;maximumFalsePositiveRate:number;maximumFalseNegativeRate:number;maximumBounceRate:number;requiredProviders:readonly string[];}
export const evaluateLaunchGate=(providers:readonly ProviderGate[],t:GateThresholds)=>{const by=new Map(providers.map(p=>[p.provider,p])),failures:string[]=[];for(const name of t.requiredProviders){const p=by.get(name);if(!p){failures.push(`${name}:missing`);continue;}if(p.samples<t.minimumSamples)failures.push(`${name}:sample_size`);if(p.falsePositiveRate>t.maximumFalsePositiveRate)failures.push(`${name}:false_positive_rate`);if(p.falseNegativeRate>t.maximumFalseNegativeRate)failures.push(`${name}:false_negative_rate`);if(p.bounceRate>t.maximumBounceRate)failures.push(`${name}:bounce_rate`);}return{release:failures.length===0,state:failures.length?"controlled_beta":"production_eligible",failures};};
